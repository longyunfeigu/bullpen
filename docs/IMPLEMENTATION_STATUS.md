# IMPLEMENTATION_STATUS.md

Overall status: IN_PROGRESS  
Current milestone: 11 (M10 + Shell v4 verified)  
Last verified commit: (see git log)

## Status legend

- NOT_STARTED
- IN_PROGRESS
- BLOCKED
- DONE
- VERIFIED

## Milestones

| Milestone | Status | Exit evidence | Notes |
| --- | --- | --- | --- |
| RAIL-D. Session Rail direction D — grouped activity rail (ADR-0023, PIVOT-038/039 + 011r/013r2/027r2) | DONE (uncommitted) | Unit 461/461; `npm run check` clean; `session-workbench.spec.ts` 2/2 incl. NEW direction-D walk (project group header with "1 need you" badge + count → amber Needs-you row → Inbox panel → row routes to the room → collapse hides rows but keeps the badge → expand restores → working-context row routes to the Projects panel with the active recent + tree); regressions on the final build: shell-v4 3/3, home-dragref 3/3, skills 2/2, pivot-shell 3/3, shell-v3 5/5, p3-full-mode 2/2, home-v2 2/3; screenshot walk 3 shots reviewed (sessions grouped/inbox/projects — /tmp/ui-shots/raild-\*.png). Pre-existing failures reproduced on clean HEAD via checkout-HEAD A/B: home-v2 PIVOT-012/015 (advanced criteria missing from the charter) and external-cli 4 (editor terminal dock: session bar, open-room button, resume echo) — all predate this work, unrelated to the rail | Mockup-first (docs/design/sidebar-vnext-gallery.html, four directions; user picked D = C's activity-bar shell × B's project grouping). SessionRail becomes a 46px activity bar (Sessions / Inbox badged / Projects / Search·⌘K + Editor / Settings) driving one context panel. Sessions panel: project groups (display-name keyed), collapsible with tree indent, per-group needs badge + count, rows drop the project name; settled sessions (ACCEPTED/ROLLED_BACK/CANCELLED) fold into a default-collapsed cross-project History group — attention states never move there, and the open room's group auto-expands (accept keeps the selected row visible; localStorage-persisted collapse). Amber Needs-you row ⇄ Inbox destination are one queue (PIVOT-013r2: Inbox opens the triage panel; row → room). Projects panel hosts recents/tree/Open folder/New project (PIVOT-011r; switching projects auto-returns to sessions). Resident working-context footer row → Projects panel. Selection = B's inset accent bar; ⌘1-9/⌘[⌘] order mirrors the visual order (History last). Restored contracts the recent shell rework had silently broken: `home-settings` (home-v2/shell-v4/skills) and `home-task-ticker-*` (shell-v4 heartbeat). Spec updates are navigation-only; no assertion weakened. Honest limits: grouping by project display name (basename collisions merge visually); History bounded by the rail's 20-task window; collapse state is renderer localStorage, not layout.save |
| PREVIEW-ROOM. Live preview as the Room's window — persistent rail, element pick, console self-heal, Full-mode reach (ADR-0022 am.2) | DONE (uncommitted) | Unit 459/459 incl. 9 new console-policy (dedupe, write-attribution window, one-send-per-burst + 5/run cap, format, write-tool detection); `tests/e2e/room-preview.spec.ts` 2/2 (Full-auto ACCEPTED task → badge lights only after a server starts, layout unmoved → open rail → element Pick injects picker, click `#hint` inside the iframe → composer chip with the selector → closed task seeds a follow-up whose first run acks `[received 1 image attachment]`; running task → console `console.error` surfaces on the ⚠ chip → manual send lands `[Preview console]` steer → Draw region → chip → same-run fix acks the image); `preview-gate.spec.ts` 5/5 re-green after the gate adopted the shared Draw mode; p4/shell-v4/p3-full-mode/room-reviewbar regressions green; visual walk `ui-shots-room-preview.spec.ts` 5 shots light+dark reviewed (`/tmp/ui-shots/rp-*.png` — Full-auto task with live rail + badge, pick-armed hint, selector chip in composer); `npm run check` + production build clean | Reframes ADR-0022: the preview is the Room's window, not the gate's evidence — driven by a user who couldn't find it twice (buried in the gate; Full mode never opens a gate). One `LivePreview` component, two variants: the Room's resizable `RoomPreviewRail` (any task state) and the gate Preview tab. Detection lights a header `PreviewBadge`; the rail opens on click and shares the peek's side slot. Feedback is point-and-say: Pick (S) injects a self-cleaning element picker via `webFrameMain.executeJavaScript` into ONLY the task's loopback frame (poll-for-commit; falls back to the zero-injection Draw marquee on any failure), posting `{selector, rect, text}` to a port-origin-filtered `message` listener; Draw (R) is the marquee. In the rail the selection becomes a composer attachment chip (`draftStore.previewRefs`) — one input, one conversation, same steer loop; after a task closes it seeds a follow-up (`task.start` v2 `preview`). Console self-heal: zero-injection relay (`preview.console` from the window's own `console-message`, loopback-filtered) + pure renderer policy (`preview-console.ts`): `auto` forwards errors only while running AND within 15s of the agent's own write, one per burst, capped 5/run; `manual` = ⚠ chip + one-click; `off` = count. Setting `preview.consoleToAgent`. Honest limits: auto-forward's DECISION is unit-tested (9 cases) and capture+manual-send is E2E-tested; the live auto-timer is their composition (staging "running + write + error within 15s" against the fast mock is racy, not covered E2E). Picker injection is the first code Charter runs inside a preview page — fenced to the task's own port, posts-only, self-cleaning. |
| PREVIEW-GATE. Visible acceptance — Preview/Checks tabs in the gate, marquee feedback, PR draft (ADR-0022; cmux 移植研究 ④) | DONE (committed b37d71d/25e9f6e) | Unit 449/449 incl. 17 new (lsof listener/cwd parsing, loopback-only binds, worktree-vs-main attribution incl. sibling-prefix footgun, webish heuristic, PR branch naming, verification matrix superseded/stale/unverified lines, command quoting + no-`&&` GIT-007 sanity, CSP directive pin); `tests/e2e/preview-gate.spec.ts` 4/4 (a dev server cwd'd in the task worktree is listed while a main-tree server on another port is NOT; marquee → note → send → compact bubble + screenshot thumbnail on the timeline → fresh run acknowledges `[received 1 image attachment]` through the real worker protocol → plan gate → REVIEW_READY; accept → PR draft card with Goal/Changes/Verification/GIT-007 body → dismiss keeps `tl-pr-draft`, and `git log`/`branch --list charter/pr/*`/`status` prove no commit, no branch, no push; verify-fail-fix task shows both runs in Checks, old one superseded, latest passed; non-web project never grows a Preview tab while Checks stays); full E2E regression 94 passed — the 2 failures (E2E-002 editor restore, E2E-005 TS intelligence) reproduce on clean HEAD via stash, pre-existing; visual walk `ui-shots-preview.spec.ts` 11 screenshots light+dark reviewed (`/tmp/ui-shots/pg-*.png`); `npm run check` + production build clean | The gate's three tabs are three projections of one `task → main` question: code (Changes, untouched testids), checks (existing `task.verificationRuns`, VER-005/007/008 semantics presented not re-implemented), pixels (Preview). Port detection is read-only `lsof` scoped by process cwd inside the task's context root — a worktree task can never show a main-tree server and vice versa; no auto-start, honest empty state names the boundary. CSP `frame-src` widened exactly `'none'` → `http://localhost:* http://127.0.0.1:*` (csp.ts extracted + unit-pinned; iframe sandboxed `allow-scripts allow-same-origin allow-forms`; will-navigate/webview/window.open/permission hardening unchanged). Marquee feedback rides the SAME request-fix steer loop (no second comment system): screenshot via compositor `capturePage`, selection burned in renderer-side, PNG persisted under workspace attachments (never in any tree), timeline `user.message` carries `{note, rect, pageUrl, thumbDataUrl}` and renders note-first with the full agent message folded; runtime contract `steer/followUp/StartRunInput` gained optional `images` end-to-end (Pi `session.prompt({images})` native — no SDK patch; mock acks deterministically). PR draft generated at accept (manual + full-mode) from the evidence ledger — goal/acceptance, merged file list ±, verification matrix, replay receipt sha256 — recorded as `task.prDraft` event + returned to the accept caller; card + timeline entry are copy-out only (`git switch/add/commit/push` + `gh pr create --draft --body-file`, one command per line, never chained, never executed — GIT-007 byte-true; `charter/pr/` prefix avoids the audit-branch collision since merge-back never commits to it). Security tightening shipped with it (the hole became load-bearing once PR drafting exists): forge CLIs `gh`/`glab`/`hub` were unclassified (R2/R3 → full-mode auto-allow could publish with stored auth); the command classifier now sends forge mutations — and unknown/`api` subcommands, fail-closed — to R4 like `git push`, keeping explicit reads (`pr view/list/checks`, `run view`, `auth status`) at R3; covered by 2 new classifier tests and re-green m7-permissions + p3-full-mode E2E. Honest limits: `task.previewOpenExternal` is loopback+detected-port only; non-web heuristic = committed package.json dev/start/serve/preview script (worktrees see HEAD, so an uncommitted script won't mark the isolated tree webish); cloud preview URLs, embedded DevTools and CI readback deliberately out (ADR-0022). |
| LEGIBLE-TERMINAL. Blocks / marker rail / sourced progress / command notifications (ADR-0021, TERM-007..011) | DONE | Unit 432/432 incl. block state machine 14 (OSC 133 lifecycle, lost-D honesty, multiline commands, rerun links, scrollback trimming, ⌘↑/⌘↓ stepping, three progress sources with 20s expiry, turn blocks), shell-integration spawn plans 11 (zsh ZDOTDIR shim incl. user-ZDOTDIR preservation, bash --init-file, fish vendor_conf.d, full degradation matrix), command notifications 7 (unfocused-only, min-duration, one-edge-one-banner, click→reveal); `tests/e2e/terminal-blocks.spec.ts` 3/3 (rail dots + ⌘↑ jump + block actions, sourced 42% on status bar/row ring + finish bell when unfocused + progress cleared with block, integration-off degradation with zero renderer errors); real-shell smoke on live PTYs: zsh A×2/B×2/C×2/D;0×1 and bash A×2/B×2/C×2/D;0×1 (bash init-file DEBUG-trap guard fixed after first smoke); terminal-adjacent E2E regressions green (m4 terminal lifecycle, quick console, external CLI; the one failure E2E-005 TS-intelligence reproduces on HEAD without these changes); boundaries + TypeScript + production build clean | Renderer-side OSC 133/9;4 parsing on each live xterm (`views/terminal-blocks.ts`, pure + host adapter) — blocks anchored by xterm markers, command text read from the buffer between B and C so any pre-existing integration also works; host-owned injection (`packages/terminal-service/shell-integration.ts` constants → written once per launch under userData, spawn plans resolved per terminal so a settings flip applies to the next spawn); unknown shell / disabled setting / missing dir spawn byte-identically to before (TERM-003). Three-surface progress paints one number: row ring ⇄ status-bar item ⇄ Dock `setProgressBar` (OSC 9;4 > conservative known-runner counters > honest indeterminate; determinate expires to indeterminate after 20s — no frozen bars). Command-finish notifications reuse PIVOT-014 hygiene (enabled-gated, PI_IDE_E2E-muted, focused-suppressed, one edge = one banner, dedup by blockId) and land the click on the block start (flash + select), not just the app; focused finishes ring the row bell instead. External sessions: observed-grade sessions mark enter/exit edges only; structured captures broadcast `external.turn` (Codex turn.completed / Claude result) as purple rail blocks — no fabricated turn boundaries. Block actions: copy output / save attachment / send to Room (reuses quick-console `draftStore.addTerminalRef`, YOU-authored, TERM-006) / rerun in the same PTY (user domain, no approval, TERM-005) with `rerunOf` links — history never overwritten (VER-005). New settings `terminal.shellIntegration` (default on) and `terminal.longCommandSeconds` (default 15) in Settings → Terminal. New channels: RPC `terminal.commandDone`/`terminal.progress`; events `terminal.revealBlock`/`external.turn` (versioned, strict). Honest limits: fish script untested on a live fish (not installed here; spawn plan unit-tested, degradation safe); observed-grade agent sessions get no per-turn blocks by design; `.prettierignore` now excludes `docs/design` (pre-existing check breakage from design-artifact commits). |
| REPLAY-V3. One story, three depths — Recap / Explore / Verify (ADR-0017 Amendment 8) | DONE | Unit 398/398 incl. projection engine 19 (per-fact evidence levels, no session upgrade, Story Time grouping/idle-folding with mandatory floors, semantic chapters ≤8 by score, interval coverage, id-backed relations only, non-coding normalized fixtures, 10k-fact perf gate), ReplayService 10 (pagination cursors, cache, evidence belongs-to-task boundary, ask fail-closed, receipt manifest hash reproducible), renderer registry/filters 6; `tests/e2e/replay-v3.spec.ts` 4/4 (result-first no-autoplay, no A–E/numeric confidence, evidence ≤3 interactions, shared fact across depth + story/real, transport + keyboard, 1024/390 no-overflow + reachable Verify receipt, cited ask with boundary, receipt HTML+JSON export, honest verification states, approval relations + timeline Verify entry, Changes-panel path anchor, Home-card entry, 10k-event ledger search/scrub, console health); adapted p2/external replay regressions green; boundary 234 + TypeScript + production build clean | Phase 1: A–E removed; one controller (`views/replay/replay-controller.ts`) shared by Recap/Explore/Verify; explicit `ReplayRequest {taskId, depth, anchor(result/fact/change/path/actual-time), liveFollow}`; categorical evidence language + measured coverage band; numeric confidence and `replayGrade()` session upgrades deleted. Phase 2: main-side `ReplayService` over the same ledger (`task.replaySession` / `task.replayEvents` paginated ≤500 / `task.replayEvidence` with belongs-to-task boundary); event-broadcast-driven refresh replaces the 2s full poll; virtualized Explore list; renderer registry incl. document (.md/.txt prose compare) and spreadsheet (.csv/.tsv changed cells) over recorded blobs. Phase 3: evidence-bounded `task.replayAsk` (deterministic ledger narrative, citations validated main-side, fails closed; boundary states what the record cannot confirm — a model pipeline can slot behind the same fail-closed contract); `task.replayReceipt` HTML+JSON export with per-row SHA-256 + manifest hash, explicitly unsigned; live follow pill; entries: Home cards (View recap / Watch live), Changes rows (path anchor), resolved approval cards → Verify, Task Room/Agent Panel buttons. Honest limits: receipts unsigned (no tamper-proof claim); per-event redaction reasons not yet recorded by the external recorder (boundary notes shown instead); pre-existing `docs/design/*` prettier failures and the documented ⌘K flake are unrelated to this work. |
| REPLAY-V2. Unified evidence replay A–E (ADR-0017 Amendment 6) | SUPERSEDED by REPLAY-V3 (substrate retained) | Full unit/integration suite 367/367; boundary 217 + TypeScript clean; production build clean; managed replay Electron E2E and real-PTY external replay E2E green; live Claude Code 2.1.210 stream-json and Codex CLI 0.144.4 JSONL smoke green; visual QA at native 1440×900 for A–E and 1024×768 for A, including external observed A/D/E | One durable event/evidence ledger now drives five projections: A default cinematic, D long-task detail, E audit/approval, B observable causal map and C cross-app view. Pi is full capture; recognized Claude/Codex JSON streams are structured; ordinary TUI sessions are honestly observed. Per-write content blobs reconstruct intermediate file versions, bounded redacted terminal evidence is durable, playback follows wall-clock time at 1–16×, and external Task Rooms expose Replay. Structured JSON is filtered before documentary persistence, so reasoning/thinking envelopes never enter replay. No inferred app relationships. |
| APPEARANCE-3. Coordinated Terminal / Archive / Index skins (ADR-0020) | DONE (uncommitted) | Full unit suite 358/358 (including Settings skin cases 5/5); m2-shell Electron E2E 3/3 (live switch + restart persistence); boundary(213) + TypeScript clean; production build clean; 1440×900 visual QA for all three skins and 1024×640 picker QA with 0 clipping/overflow and empty renderer console | `general.skin` is an independent persisted layer above existing light/dark/system brightness. Shared tokens switch shell colors, UI/display/mono fonts, shadows/radii and icon geometry; six Monaco themes, Prism semantic colors and six live xterm palettes complete the code/terminal surfaces. Archive is the backward-compatible default; custom editor fonts remain user-owned. No dependency or database migration. |
| FIX-EXTERNAL-SINGLE-RAIL. A promoted Claude/Codex terminal exclusively owns the right rail | DONE (uncommitted) | Red-before/green-after Electron E2E on a real PTY: the prior UI kept `agent-panel` mounted beside `external-panel`; the fixed flow asserts Agent Panel visible before promotion → absent after promotion (including after its ⌘L toggle command) → visible again after Return to dock. Full external CLI E2E 4/4, renderer console/page errors empty, `npm run check`, production build, and screenshot `/tmp/charter-external-panel-resized.png` green. | The external session panel and generic managed-task panel no longer coexist. Promotion records the prior Agent Panel visibility and collapses it; Return to dock, terminal close, and workspace change restore that prior layout. A render-level exclusivity guard prevents commands from opening a second rail while the external terminal owns the slot. If Agent Panel was already hidden, it is not forced open later. Managed Task flows are unchanged. |
| FIX-EXTERNAL-PANEL-RESIZE. Promoted Claude/Codex panel has a usable horizontal splitter | DONE (uncommitted) | Red-before/green-after Electron E2E on a real PTY: original splitter hit box measured 5px and failed the ≥12px usability gate; fixed flow promotes the terminal, drags the boundary 100px across Monaco/xterm, asserts panel width changes by >60px, then focuses the separator and resizes with ArrowLeft. Full external CLI E2E 4/4, renderer console/page errors empty, `npm run check`, production build, and screenshot `/tmp/charter-external-panel-resized.png` green. | Root cause: the boundary was a 5px absolutely-positioned hairline and used bubbling window `mousemove` listeners; it was extremely hard to acquire and could lose movement over Monaco/xterm. It is now a 12px hit target with Pointer Capture, persistent resize cursor/selection suppression, hover/focus/drag affordance, clamped 480–900px width, ARIA range metadata, and Left/Right/Home/End keyboard control. |
| FIX-EXTERNAL-RESUME. Claude/Codex external sessions resume through their CLI instead of the managed agent runtime | DONE (uncommitted) | Real Electron + PTY E2E 4/4 green: ended Claude → Task Room → Resume writes `claude --continue` into the same terminal, returns the same Task to IN_PROGRESS, and creates no duplicate Task; crash/relaunch E2E: orphaned external Task recovers to REVIEW_READY, Resume creates a terminal in the recorded cwd and continues the same Task; existing accounting/room and native-installer detection regressions remain green. Command-map + IPC contract unit tests 9/9, `npm run check`, production build, and live persisted-data recovery green (the user's historical `external=ended + Task=INTERRUPTED` row normalized to REVIEW_READY on startup). | Root causes: restart recovery first projected external work as generic INTERRUPTED; session finish only closed IN_PROGRESS; older builds could leave an already-ended external row stranded as INTERRUPTED; the generic Resume button called `task.start`, which intentionally rejects external Tasks; Terminal → Room navigation did not bind `activeTaskId`. External metadata now records cwd, the host owns a fixed known-CLI command map (`claude --continue`, `codex resume --last`), resumes in the original live terminal or a new terminal at the recorded cwd, waits for real process detection before reporting success, and keeps the original Task/baseline/change set. Startup also repairs historical ended/incomplete split-brain rows. Unsupported/custom CLIs do not get an executable Resume action. |
| FIX-HOME-TERMINAL. Home bottom-panel actions reveal the Editor | DONE (uncommitted) | New focused E2E: Home → command palette → click New Terminal → Editor terminal dock visible and xterm mounted; Home → ⌘J → bottom panel visible. E2E-007 terminal lifecycle regression remains green; production build clean | Root cause: Home is a fixed overlay over the workbench, while `showBottomTab` and `toggleBottomPanel` only changed the hidden Editor layout. Successful bottom-panel actions now reveal the Editor before opening the panel; terminal creation failures still remain on Home and surface their existing toast. |
| SKILLS-LIVE. External Agent/Codex/Claude skill discovery + live sync (ADR-0019) | DONE (uncommitted) | `skills.spec.ts` 2/2 E2E: original managed import/audit/slash flow plus isolated fake-home Agent Skills source → discovered Off → trust + auto-enable → watcher-driven SKILL.md update/new skill/delete reflected in the already-open Settings UI and slash picker; 25 focused skill/IPC unit tests; full 355 unit tests; full E2E 79 passed + 9 environment-gated/skipped; `npm run check` + production build clean. The repo-level `npm run test:security` remains non-runnable because the pre-existing M11 `vitest.security.config.ts` is absent; feature security cases run in the normal suite. | `SkillStore` is now a derived multi-source catalog: managed snapshots + `~/.agents/skills` + `~/.claude/skills` + `~/.codex/skills` + custom connected roots. External discovery is default-Off; source trust and auto-enable-new policy persist in lenient `skills.json` v2 while legacy managed ids/flags survive. Watcher events debounce to full scans; startup/on-demand/focus/45s reconciliation closes dropped-event gaps. External add/update/delete/rename follow the source directly; custom Disconnect never deletes originals; Import Copy stays isolated. Stable source/path ids, deterministic `name@source` conflict qualification, provenance/live/invalid/needs-review/revision UI, block/folded frontmatter support. `realpath` containment closes skill-root/nested-symlink escapes and retarget races in both audit and load_skill; invalid size/link revisions fail closed. Successful loads include source, catalog revision and exact content SHA-256 in results (source+revision also land in the persisted tool summary). Reused sessions receive a current catalog refresh each run/reply; Pi adapter/runtime contract unchanged. E2E only scans external roots when an isolated `PI_IDE_SKILLS_HOME` is supplied, never the developer's home. |
| UX-ROOM-E. Room conversation presentation — preset E「手记工作台」(ADR-0018) | DONE (uncommitted) | real-room-e.spec 1 REAL-gateway E2E (agent write task on anthropic/claude-haiku-4-5 with a substantial Chinese brief → worklog groups with mm:ss clocks visible mid-run → plan/permission gates auto-decided → REVIEW_READY; docs/NOTES.md + src/util.ts `mul` asserted ON DISK; follow-up reply run updates the file again「更新于第二轮」and yields a second worklog group; computed-style assertions: spine ::before 1px, agent prose font contains Charter, milestone label uppercase, reply textarea outline none; screenshots /tmp/ui-shots/e-\*.png); mock-runtime regression m6-agent-ask 4 + room-peek 2 + room-reviewbar 2 all green; 345 unit tests; `npm run check` clean | Mockup-first (docs/design/room-conversation-directions.html — four pure directions A–D + hybrids E–G over one 37s scripted animated run, five mixable skin dimensions via a Tweaks panel; user picked preset E 1:1). room.css + RoomTimeline.tsx only: centered 640 column with a left time spine (milestone glyphs mask onto the rail; labels mono-uppercase with trailing hairline), agent prose serif (--font-display 15px/1.9, inline code pinned 11.5px), contiguous tool/verification/setup rows grouped into recessed mono worklog blocks (`tl-worklog`) with clocks counted from the room's first event + flash-in (reduced-motion aware; null-rendering events never break a group, cards/milestones/bubbles do), review bar + composer ride the same column, activity strip becomes a mono console ticker. Composer focus bug root-caused and fixed: theme.css global :focus-visible (near-black --focus-ring) beat `.tr-cinput{outline:none}` because main.tsx imports CSS after components (ESM hoisting → theme.css evaluates last; equal specificity → later wins) — now `.tr-cinput:focus-visible{outline:none}` + focus expressed on the card (border tint + 3px ring), matching Home. Row testids unchanged; Editor agent panel untouched |
| EXTERNAL-CLI-REV2. External session interaction redesign —「原地装饰、意图升格」+ xterm mount substrate (ADR-0017 Amendment 2) | DONE (uncommitted) | external-cli.spec 2 E2E rewritten & green (detect = bar/badge only, dock untouched → user "Move to side panel" → scrollback follows + typed keys echo via PTY → session end keeps placement (panel ended state) → "Return to dock" restores pane+scrollback → bar ended + Review → room/peek; native-installer shape re-fixtured on a node-binary copy — the old /bin/zsh copy is AMFI-SIGKILLed and had voided that spec); NEW terminal-remount.spec (tab A→B→A, Home⇄Editor round-trip, still-live echo); REAL-CLI e2e 3/3 (claude 2.1.210 interactive incl. /exit + return, claude -p accounting → REVIEW_READY, codex 0.144.1 interactive incl. update-menu/trust prompts driven by keystrokes); full e2e 76/76; 346 unit; check clean | Mockup-first (docs/design/external-cli-attach-redesign.html — ~43s interactive film, user approved). Root cause of the shipped-broken 决策 4: xterm 6 `open()` attaches only on the FIRST call — every `innerHTML=''; open(host)` re-mount (promotion, dock tab switch, ⌘E round-trip, return) left a detached element = blank pane, dead input; `external-cli.spec.ts:89` was red at HEAD. Fix: shared `mountTerminal()` (first open, then `replaceChildren(term.element)` + fit + refresh + focus) used by dock/panel/room. Interaction: detection only decorates (tab badge + 34px session bar: snapshot chip, live counter, ⤢ Room, ⇥ Move to side panel; one toast); promotion = user click (slot-exclusive; pref `terminal.autoPromoteExternal` default off restores auto); panel 600px default (≥80 col TUI floor; was 350px ≈ 45 col), drag-resizable 480–900, ⇤ Return anytime; session end changes state (bar/panel ended + Review) but never placement; bar/room entry persist after end until the terminal closes or a new session replaces them; unpromote hands dock `active` back (fixes the returned-blank bug); kill/workspace-switch clear session UI state. Security semantics, accounting, room form, REVIEW_READY unchanged |
| EXTERNAL-CLI. External CLI agent sessions — embedded claude/codex terminals as first-class tasks (ADR-0017) | DONE | external-cli.spec 1 E2E against a REAL PTY (fake agent node script on PATH → detection badge ✳+EXT → "Open session room" → terminal center column in the room → watcher-accounted rail row → peek on the diff → session end → REVIEW_READY, changeSet honest vs the entry snapshot: modified +1, disk edit real); new unit tests: agent-detect 7 (tracker debounce, title/command matching), git snapshotTree/readTreeBlob 2 (temp index: dirty+untracked covered, real index untouched, .gitignore honored), external baselines 4 (bytes-baseline vs disk, created-null, idempotence, byte-exact rollback), isAccountablePath 3 — 337 unit total; `npm run check` clean; full e2e 69 passed + 1 known ⌘K load-flake (passes standalone) | Mockup-first (docs/design/external-cli-final-mock.html — composition of the two direction studies external-cli-terminal-directions.html × terminal-placement-directions.html; user approved). Detection: `pty.process` foreground-title polling (700 ms) + argv process-tree fallback whenever the title is neither a known CLI nor a shell prompt — covers node/bun interpreters AND the native installers' version-named binaries (`claude → versions/2.1.209`; ADR-0017 amendment 2026-07-15), one shared `ps` snapshot per tick, exit debounced ×2, `PI_IDE_EXTERNAL_CLIS` override; TerminalManager.onAgentState (kill/exit fire the edge too). Session start: `git write-tree` against a TEMPORARY index (untracked covered; user's index untouched; non-git degrades to first-seen baselines) → createExternalTask (mode ask, model external/<cli>, migration v3 `external_json`; startTask refuses external tasks — they never dispatch an agent run) → per-session WorkspaceWatcher; first touch of a path captures `ensureBaselineFromBytes` from the snapshot blob, so net change set, review presentation and byte-exact rollback are the EXISTING CHG machinery unchanged. Renderer: externalStore (toasts, terminal-tab ✳ badge + EXT chip + "Open session room" entry, session-end summary line in the scrollback); synthesized activity 'write' items feed the shared presence machinery (tree/task glow, rail filesTouched, peek pulse-follow). External Task Room form: the session's terminal (same xterm instance re-parented, PTY uninterrupted) replaces timeline+composer; EXT·unmanaged + snap-ref header, LIVE·following pill (peek auto-follows the file being written; click to pin), diffstat rail rows; Stop/Replay/composer hidden; end → REVIEW_READY (never auto-ACCEPTED; zero-change → Answered per PIVOT-031); app-quit strandings swept to review on startup. Bounded v1 per ADR-0017: no transcript parsing (codex app-server / claude stream-json recorded as the end-state path), no composer→stdin bridge, worktree terminals excluded from accounting |
| FIX-INIT. Renderer init idempotence (StrictMode duplicate IPC listeners) | DONE (uncommitted) | init run-once 2 unit tests (320 total); `boundaries+tsc` clean; dev relaunch log free of MaxListenersExceededWarning (was: 12 `evt:workspace.changed` listeners, warning at 11); m2-shell + m3-editor E2E 5/5 (workspace.changed → editorStore reset/restoreTabs exercised) | Root cause: Workbench mount effect drained `initRegistry` with no run-once guard while React.StrictMode double-invokes dev effects; 4 of 8 `workspace.changed` subscribers (workspaceStore, editorStore, m4 inline, wirePython) lack per-store `initialized` guards → registered twice. Fix: registry + `runInitsOnce()` extracted to workbench/init.ts (module-level guard, returns whether this call initialized so the ADR-0013 gitStatusStore import stays gated); Workbench re-exports `initRegistry` so contrib imports are unchanged. Dev-only symptom (prod React doesn't double-invoke; 8 listeners < 10 cap) but dev double-execution of reset/restoreTabs/loadProject/lsp.status is gone |
| UX-ROOM-ENDING. Room completion as review bar + reply model/effort override (ADR-0016) | DONE | room-reviewbar.spec 2 E2E (bar lifecycle: Done milestone, evidence meta incl. unverified flag, review-open round-trip, overflow rollback → bar vanishes; reply re-points model → tl-model-changed note → next run → composer pill agrees); soak 50 laps green through the bar's overflow rollback; regression pivot-shell 3 + shell-v4 5 + m6 4 + m9 3; mock setSessionModel unit test (321 total); boundaries+tsc clean | Mockup-first (docs/design/room-ending-directions.html; user picked direction B + reply-composer model parity). Room-only presentation change — the Editor agent panel keeps its dense report card. `report.final` renders as a quiet ✓ Done milestone; a review bar docks above the composer while REVIEW_READY ∧ ¬answered (recorded evidence only: changed ±, checks incl. superseded/stale, unverified/risk warn rows; primary Review changes; agent summary + double-confirmed rollback in the overflow) and vanishes with the state. The mid-task reply composer gains the shared ModelEffortControl (trust level stays read-only — the session's tool catalog is mode-scoped at creation); `task.message` carries an optional ModelRef applied runtime-first (pi AgentSession.setModel + clamped setThinkingLevel; mock records it; new setSessionModel worker req/resp), then persisted to tasks.model_json + a `task.modelChanged` timeline note; launches re-assert the task model on reused sessions (stale/lost sessions recreated). A failed switch rejects the send loudly — nothing is ever sent on the wrong model |
| SKILLS. Managed skills + "/" invocation (ADR-0015) | DONE (uncommitted) | skills.spec 1 E2E (seeded store → "/" picker filter/insert → /skill: task through mock runtime → Settings manager toggle/audit → Off skill vanishes from "/" immediately); 15 new unit tests (skill-store 10: frontmatter/import validation+limits/slug collision/enable round-trip/preamble excludes explicit-only/expansion incl. newline args+disabled pass-through/traversal+binary guards/remove; tools-skill 5: R0-in-ask/reference files/traversal/unknown-names-enabled/binary); 318 unit total; `npm run check` clean; real-app screenshot walk reviewed (picker, filter, insert, manager, audit, toggle) | Mockup-first (docs/design/skills-manager-mockup.html, user-confirmed two-state Off/Auto). AG-014-preserving design: skills live in a managed store (userData/skills; AppPaths.skillsDir) imported explicitly from local folders — project dirs are NEVER scanned. Pi's native skill injection is read-tool-gated (unusable under TOOL-001), so loading is product-owned: R0 gateway tool `load_skill` (name+optional bundled file, traversal-guarded, timeline-audited = AG-014 auditability), `<available_skills>` in buildPreamble (enabled ∧ ¬explicit-only), and `/skill:name` expanded in task-service (launch + steer/followUp; timeline keeps the user's raw text) — zero pi-adapter changes, works with the mock runtime. Frontmatter `disable-model-invocation` honored (explicit-only badge; still expandable via "/"). 5 IPC channels (skills.list/import[dir seam]/remove/setEnabled/read), SkillStore (SecretService-style folder store, 500-file/20MB import caps), Settings → Agent → Skills manager (Off/Auto segment, audit view with script highlighting + Permission-Engine note, two-step remove via confirm), composer "/" picker shared by Home + Task Room (enabled skills only, filter-as-you-type, explicit-only badge). Toggles apply to running sessions at next load_skill call (provider resolves live); preamble snapshot is per-session |
| SHELL-V5. Room zoom continuum — in-room file peek + ⌘E continuity (ADR-0014, PIVOT-034..036) | DONE | room-peek.spec 2 E2E (rail/timeline → peek beside the conversation, diff+file modes, Esc restore, explicit escape hatch → Editor tab; draft+peek survive room→Editor→room); shell-v4 worktree test extended (peek File mode reads WORKTREE content, escape hatch hidden); peek reducer 10 unit tests (313 total); `npm run check` clean; affected suites green (shell-v3 5, shell-v4 3+, home-v2, home-dragref, p2-visibility) | End state per ADR-0014: one room, three zoom levels, conversation is the anchor — no plain file click leaves it. New `task.peekFile` channel → `contextForTask().documents.readLogical` (mount-honest: worktree/non-focused projects/live buffers; binary/missing/1MB-truncated flags). `appStore.peek` (pure logic in views/peek.ts) + `FilePeek` split panel (Changes hunks reuse FileLens renderer; File mode = read-only Monaco, pulse-following); rail rows/in-room LiveBoard tiles/timeline evidence paths (now clickable)/⌘K-report chips via openWorkspaceFile/Home tree all default to peek in-room, ⌘/alt-click = explicit Editor; worktree tasks hide the hatch. ⌘E room-aware (openTaskInEditor shared with the header button); per-task reply drafts (draftStore) + timeline scroll memory (scrollMemory, pinned-to-bottom aware) shared by RoomTimeline and the Editor panel. Deferred by design: spot-edit in peek (needs attribution ADR), PIVOT-037 shell unification |
| UX-COMPOSER. Follow-up composer parity + merged model·effort control | DONE (uncommitted) | `npm run check` clean (prettier+boundaries+tsc); 293 unit tests; affected e2e green (home-v2/pivot-shell/p2-visibility 8/8); real-app screenshot walk reviewed (Home + follow-up composers, both popovers; effort correctly greyed for unsupported levels) | Mockup-first (docs/design/composer-followup-model-effort-mockup.html, user-confirmed): (1) model + reasoning effort merged into ONE popover pill — shared `ModelEffortControl` (views/ui.tsx): provider-grouped model rows + an effort segment scoped to the selected model's supported levels, clamp-on-switch — replacing the two free-floating `home-thinking`/`home-model` selects so effort always reads against its model. (2) The Task Room follow-up (closed→new task) gets full Home-composer chrome (project chip, @-attach picker, trust segment, merged pill, send) seeded from the finished task's mode/model but freely editable; running/plan-awaiting states keep the bare reply pill (nothing to re-pick mid-run). e2e `home-model` readiness migrated select→pill (`toHaveValue`→`toContainText`); real-gateway `selectOption`→popover click. No mic button (app has no voice input — would be fake UI) |
| UX-DRAGREF. Sidebar New project entry + drag-to-@ context feeding | DONE (uncommitted) | home-dragref.spec 3 E2E (sidebar dialog from Launcher AND from a Task Room; tree→composer file+dir chips with dedupe landing in the charter; tree→room reply inline @ at the caret); regression home-v2/shell-v3/shell-v4/pivot-shell/p3-light-edit 18/18; `npm run check` clean; screenshot walk reviewed | Mockup-first (docs/design/sidebar-newproject-drag-at-mockup.html, user-confirmed): (1) Projects section gains a New project… row (below Open folder…, which now carries the folder icon) opening NewProjectDialog — open state moved to appStore and rendered by HomeShell so the entry works beside a Task Room too. (2) HomeProjectTree rows are drag sources (views/dragRefs.ts: `application/x-charter-ref` MIME, dirs get a trailing `/`, `text/plain` `@path` fallback); the Home composer's onDrop short-circuits internal drags into addRefs (no relativize round-trip; dir chips render the folder icon), and the Task Room reply accepts the same payload by inlining `@path ` at the caret with a drop ring on the pill (`tr-composer.drop`) |
| P4-REVIEW. Review v2 + git decorations (ADR-0013) | VERIFIED | p4-review-v2.spec 2 E2E (Monaco diff + hunk decisions + request-fix round-trip; explorer/tab/gutter decorations on a git fixture); m8-review 4 + m9 3 + shell-v3 pass UNMODIFIED against the new surface; gutter-diff parser 5 unit tests; full suite 60/60 | Review overlay re-rendered as Changes list (A/M/D/R ± counts, file accept/reject) + Monaco side-by-side diff (baseline snapshot vs current via new task.reviewFile — ChangeService blobs, never git); hunk strip keeps content-derived keys/testids/hash guards byte-identical; “Request fix” sends selected lines + note back as steer and reopens the room; shared gitStatusStore → explorer letters+folder dots, tab marks, gutter change bars vs index (refresh on watcher/save); composer mode segment compacted (Read/Approve/Auto/Full) after a visual-walk audit |
| P3-FULL. Full autonomy mode (ADR-0012) | VERIFIED | p3-full-mode.spec 2 E2E (R3 without pause → auto-ACCEPTED → post-accept rollback byte-exact; failing verification pauses auto-apply); permission-engine full-mode unit test; task-machine ACCEPTED→ROLLED_BACK test; full suite green | 4th mode 'full': composer danger segment, R1–R3 auto-allow (standing denies + R4 + gateway boundaries untouched), plan auto-approve, finalizeRun auto-accept (actor system:full-auto) with honest fallbacks (failed/timeout verification → REVIEW_READY + AUTO_APPLY_SKIPPED + attention ping; merge conflicts → merge-blocked); ACCEPTED→ROLLED_BACK transition + Task Room post-accept rollback (non-worktree); notifications: quiet pass-through, 'Completed & applied' on ACCEPTED; spec §4/§6.1 + CLAUDE.md amended |
| P2-VIS. Thinking + live activity (ADR-0011 amends AG-006) | VERIFIED | p2-visibility.spec 2 E2E (collapsed thinking → expand → evidence exclusion; activity strip narrates); full suite green | thinking.delta/completed contract events; pi adapter forwards pi thinking streams per block (was dropped); mock 'thinking' step in ask-basic; settings.agent.showThinking (default on) gates stream+persist; room: shimmering live block folds into 'Thought for Ns' row; agent panel card; activity strip (pulsing dot + current action + tool elapsed + ↑↓ tokens); excluded from action lines/pulses/report evidence |
| P1-QOL. Markdown prose, live tree, effort×model, new project, worktree supply (ADR-0010, ADR-0009 am.2) | VERIFIED | project-create 7 + worktree-include 11 unit tests; shell-v4 branch-chip assertions migrated; full suite green after each batch | Shared <Markdown> (react-markdown+remark-gfm pinned, no raw HTML, monaco-colorized fences, checked external links) across bubbles/plan/report/question; HomeProjectTree shares workspaceStore (fs.batch live refresh) + mergedBack glow pulses; ThinkingLevel+xhigh & per-model supportedThinkingLevels (pi-ai pinned) with hidden-unsupported composer select + runtime clamp; workspace.createProject (empty/git-init/clone non-interactive) + Home dialog; TasksView segmented restyle; worktrees: .worktreeinclude copy, setup command (lockfile suggestions, timeline evidence, fail-fast discard), slug branches, chip menu (terminal at worktree cwd, reveal), Changes→FileLens, orphan sweep, missing-worktree degradation |
| --- | --- | --- | --- |
| UX-POLISH. User-audit fixes (task cleanup, effort in composer) | DONE (uncommitted) | Full suite 54/54 (soak/real-gateway/shots skipped); 268 unit tests; `npm run check` clean; manual visual walk (/tmp/explore2) | From a user-hat audit of the shipped shell: (1) task cleanup — `task.archive` finally has UI: hover archive on sidebar rows, archive on RECENT cards, Archive… (two-step) in the Task Room header; answered tasks close out (accept-the-no-op) then archive; running/plan-waiting/unreviewed tasks never offer it (labels.canArchiveTask). (2) Reasoning effort is a composer control (`home-thinking`, ✦ levels) — Settings → Models → default thinking level was a dead setting (never applied; runtime silently used 'medium'): now seeds the composer and every creation path passes model.thinkingLevel end-to-end; room meta line shows `· effort …`. (3) Home RECENT section — finished work (Accepted/Answered/Rolled back/Cancelled) stays one glance away instead of a dead-empty launcher. (4) Composer seeds mode from Settings → Agent (was hardcoded 'edit'). (5) ⌘K fixes: task badges use presentedMeta (Answered, not raw Review), New Task lands in the composer even from a room, Open Settings no longer yanks to the Editor surface. (6) `task.new` command + File-menu accelerator → ⌘N focuses the composer (old ⌘⇧I opened the legacy dialog); sidebar shows the hint. (7) Composer bottom-row overflow fixed (send button was clipped outside the card once effort joined; selects shrink with ellipsis, seg padding trimmed) |
| PROVIDERS. Multi-provider registry (ADR-0009 am.1, PIVOT-033) | VERIFIED | providers.spec E2E (presets + custom gateway + required-URL validation + independent delete); 8 model-catalog unit tests (anthropic/openai/openrouter/litellm protocols, evict); real-gateway spec ×2 live: Anthropic protocol AND OpenAI protocol (LiteLLM preset → 25 models fetched → real PONG via litellm/claude-haiku, screenshot real-4-litellm.png) | Presets Anthropic/OpenAI/OpenRouter/LiteLLM + custom Anthropic-/OpenAI-compatible gateways; protocol/endpoint/name = non-secret meta beside keychain key (legacy entries infer protocol); effective endpoints resolved in main; pi adapter registers non-builtin providers lazily with wire protocol + apiKey (registry requirement); Home picker groups models per provider; catalog evict on delete |
| SHELL-V4. Global tasks, multi-mount, worktrees (ADR-0009, PIVOT-028..032) | VERIFIED | shell-v4.spec 5 E2E (persistent shell; cross-project approve/finish while another project is focused; worktree isolation + merge-back with main-tree byte checks; composer request-changes → plan v2; heartbeat ticker + rail focus board); full suite 53/53 (soak/real-gateway skipped); 265 unit tests (light-completion presentation, answered notification copy) | Engine: per-root ProjectContexts (gateway/permissions/changes/verification per mount — closes the cross-project gateway-rebind hazard), WorktreeService (create/discard/merge-back with baseline conflict preflight), task.create{projectPath,isolation}, task.list scope=all, TaskDto{project,changedFiles,worktree}, migration v2, plan request_changes channel (works for real runtimes via PLAN_CHANGES_REQUESTED tool result), pi adapter now delivers the system preamble (identity + mode rules were silently dropped before). Shell: persistent sidebar (grouped global tasks, action ticker heartbeat, Inbox, project badges), Task Room v2 (project·branch chip, rail THIS-TASK LIVE board, timeline v2 mockup language, plan-aware composer), light completion (Answered/Done, excluded from Needs You) |
| 1. Engineering baseline | VERIFIED | E2E m1-baseline (isolation+IPC), 28 unit tests, packaged .app launch smoke, boundary lint | node:sqlite/node-pty/rg strategies in ADR-0003 |
| 2. Shell and persistence | VERIFIED | E2E m2-shell (restart restore), m2-db-failure (safe mode), 10 unit tests (db+settings) | |
| 3. Workspace and editor | VERIFIED | E2E-002/003 pass (m3-editor.spec), 31 unit tests (doc store/workspace/path boundary), 10k-file lazy tree integration test | Data-loss race renderer-guard documented in editorStore |
| 4. Search, LSP, terminal | VERIFIED | E2E-004..007 pass, 6 search unit tests, full E2E suite 12/12 twice | rg via system binary (ADR-0003); python LSP degrades with guidance |
| 5. Git and changes | VERIFIED | E2E-008 + non-git flow, 51 unit tests incl. 34-case rollback matrix | Full 50-case matrix expands at M12 gate run |
| 6. Pi read-only agent | VERIFIED | E2E-009/019-core/HIST-002 (4 E2E), pi adapter contract tests (no-builtin-tools), 141 unit tests total | OAuth providers deferred to API-key flow (ADR pending M12 notes) |
| 7. Tools and permissions | VERIFIED | E2E-012/013 + approve-and-run + ask_user (4 E2E green), permission engine 15 tests, classifier 17, runner 11, command tools 9, security matrix 9, SQL store 3 | R3 never granted permanently; R4 refused pre-prompt; PERM-007 param-binding enforced |
| 8. Agent editing and review | VERIFIED | E2E-010/011/014/015 all green (m8-review.spec, full suite 26/26), 25 new unit tests (review hunks 10, write tools 10, plan utils 5) | Plan approval flows through the propose_plan tool channel (works for real Pi too); write gate denies first write until plan approved (AG-007); hunk keys are content-derived so decisions fail closed on staleness |
| 9. Verification and history | VERIFIED | E2E-016/017/018 green (m9-verification.spec, full suite 29/29 twice), VerificationService 7 unit tests, rename tool test | Final report separates agent narrative from system evidence; stale/superseded semantics on verification_runs; unverified accept needs explicit confirm; rollback preflight stops on external-change conflicts |
| PIVOT. Dual-form shell (ADR-0004) | VERIFIED | pivot-shell.spec 3 E2E (PIVOT-001..009), full suite 32/32 twice, model-catalog 4 unit tests | Charter branding (no Pi in UI); Home task launcher default entry; Settings provider keys + live model fetch |
| HOME-V2 P1. Activity stream + mission control (ADR-0006) | VERIFIED | home-v2.spec 3 E2E (PIVOT-011..015), full suite 35/35 twice, activity projection 9 + notification 4 unit tests | Pure-projection activity stream (live = replay source); mission-control cards with live current action; macOS notifications on attention edges (focused-window suppressed); drag/@ context refs; clickable paths; tool lifecycle renders as one in-place card (callId dedupe) |
| HOME-V2 P2. Parallel runs + replay + glow + ⌘K (ADR-0006) | VERIFIED | p2-parallel-replay.spec 3 E2E (PIVOT-016..018), full suite 38/38 ×3 consecutively, gateway modeForTask unit test (252 total) | maxConcurrentRuns (default 3, FIFO beyond); per-task mode resolution replaces the mutable gateway.mode; action-centric session replay (scrubber + stored per-step patches + file lens, read-only); presence glow on tree/task rows from change events (no polling); ⌘K over projects/tasks/files/actions |
| HOME-V2 P3. Light editing (ADR-0007, PIVOT-019/020) | VERIFIED | p3-light-edit.spec 2 E2E, full suite 40/40 ×3 consecutively after fixes | Rich Markdown via @mdxeditor/editor@4.0.4 (opt-in per file + setting; writes through the SAME Monaco model → identical dirty/conflict/save guarantees; lazy-loaded + error boundary; Prism global shim). In-house canvas image annotator (arrow/box/true-pixel mosaic, save-copy never overwrites, attach-to-charter). Root-caused the earlier full-run flakes: (1) E2E runs were emitting real macOS notifications — now silenced under PI_IDE_E2E; (2) ⌘K hover-on-render stole keyboard selection when the physical cursor overlapped the palette — onMouseEnter→onMouseMove; (3) rich-editor mount settle window swallowed instant edits — replaced with a normalization-baseline guard |
| SHELL-V3. Task-centric shell (ADR-0008, PIVOT-021..025) | VERIFIED | shell-v3.spec 3 E2E (Task Room flow, entry consolidation, Live Board); full suite 46/46 ×2 (soak skipped); state-labels 5 + live-board 4 unit tests (262 total) | P1 humane vocabulary (shared dictionary, no raw enums/emoji; data-state for tests), danger hierarchy (two-step rollback, Cancel-task demoted), review-overlay collision root-caused (undefined --bg); P2 Studio tokens ported 1:1 from direction-picker.html (light+warm dark, serif hero anchored to composer, segmented trust control + caption, quiet status bar); P3 Task Room on the Home surface (timeline/cards shared with the Editor panel via TaskTimeline exports; changes+verification rail; decision panel; Review/Replay global overlays; submit→room, no auto-jump; Editor entries: sidebar row + ⌘E + room header); P4 Live Board (event-driven tiles: ripple/heat 60s decay/rhythm/writing beacon; per-task boards; diff-so-far FileLens with pulse-following refetch; pauses on blur/reduced-motion; edit-live mock scenario); P5 provider base URLs (PIVOT-026: keychain key + non-secret endpoint, catalog fetch via gateway, pi adapter registerProvider + synthesized gateway model ids — verified live), Settings Studio restyle, Home project tree (PIVOT-027), Advanced Title parity (PIVOT-012r). Amendments in ADR-0008 |
| 10. Recovery and diagnostics | VERIFIED | m10-recovery.spec 3 E2E (E2E-020/022 + renderer crash), soak.spec 50-task run (opt-in), disk-write-failure unit test, full suite 43/43 ×2 (soak skipped) | Worker orphan guards (port close + ppid watchdog); ordered will-quit teardown (gates resolved → worker disposed → DB closed last, fixes "database is not open" on quit); INTERRUPTED recovery entry (Home + AgentPanel Resume/Review/Roll back); orphaned pending permissions cancelled in the event log on restart (no replay); redacted support bundle (diagnostics.supportBundle); E2E crash dialogs/notifications suppressed under PI_IDE_E2E |
| 11. Security and quality hardening | NOT_STARTED | | |
| 12. Packaging and Stable release | NOT_STARTED | | |

## Current blockers

None.

## Latest test evidence

| Date | Commit | Command | Result | Artifact |
| 2026-07-16 | (rail-d, uncommitted) | `playwright test session-workbench home-v2 home-dragref shell-v4 skills` | 17 passed, 1 failed (home-v2 PIVOT-012/015 — pre-existing, reproduces on clean HEAD with the rail files checked out to HEAD; advanced-criteria loss is in the composer/charter path, untouched here) | playwright |
| 2026-07-16 | (rail-d, uncommitted) | `playwright test pivot-shell shell-v3 p3-full-mode external-cli` + tmp screenshot walk | 12 passed, 4 failed — all 4 in external-cli (editor terminal dock surfaces); representative 2 re-run against a clean-HEAD build fail identically → pre-existing from the earlier shell rework, unrelated to the rail | playwright |
| 2026-07-16 | (rail-d, uncommitted) | `npm test` + `npm run check` | 461 unit passed; prettier + boundary + tsc clean; production build clean | vitest |
| 2026-07-16 | (rail-d, uncommitted) | screenshot walk (temp spec, removed) | sessions grouped view (needs badge, B-style selection, context footer), inbox panel, projects panel + tree reviewed | /tmp/ui-shots/raild-\*.png |
| 2026-07-16 | (preview-room am.2 fix, uncommitted) | `playwright test room-preview` | 3/3 — new "one-click dev start keeps you in the Room, no surface flip to the editor" regression + existing two. Field report: starting the dev server yanked the user into the Editor terminal, then back. Root cause: `terminal.create` default `reveal:true` → `showBottomTab` → `setSurface('workspace')`. Fix: start the dev server in a background terminal (`reveal:false`); log reachable via an explicit "Dev log" button (Replit Console pattern — never a side effect of starting). | playwright |
| 2026-07-16 | (preview-room am.2, uncommitted) | `playwright test room-preview preview-gate` + touched-surface regression | 2 + 5 + 11 passed (Full-mode rail reach, element-pick injection → composer chip → follow-up seed, console capture + manual steer, draw-region same-run fix; gate adopted shared Draw mode; p4/shell-v4/p3-full-mode/room-reviewbar green) | playwright |
| 2026-07-16 | (preview-room am.2, uncommitted) | `npm test` + `npm run check` | 459 unit passed (9 new console-policy); prettier + boundary + tsc clean; production build clean | vitest |
| 2026-07-16 | (preview-room am.2, uncommitted) | `CHARTER_SHOTS=1 playwright test ui-shots-room-preview` | 5-shot walk reviewed — Full-auto task shows a live preview rail + header badge (the exact blind spot the user hit), pick-armed hint, `#coupon-hint` selector chip in the composer, dark | /tmp/ui-shots/rp-\*.png |
| 2026-07-16 | (preview-gate, uncommitted) | `playwright test preview-gate` | 4 passed — worktree/main-tree port attribution boundary, marquee→screenshot→image-through-worker round trip, accept→PR draft with git-side-effect-free proof, checks superseded history, non-web tab absence | playwright |
| 2026-07-16 | (preview-gate, uncommitted) | `vitest command-classifier` + `playwright test m7-permissions p3-full-mode preview-gate` | 19 classifier unit (incl. forge R4/R3 split) + 10 E2E passed — gh/glab/hub mutations now R4 (GIT-007 class), reads stay R3; full-mode auto-allow can no longer publish | vitest+playwright |
| 2026-07-16 | (preview-gate, uncommitted) | `npx playwright test` (full e2e) | 94 passed, 2 failed (E2E-002, E2E-005) — both reproduce on clean HEAD via `git stash` (pre-existing, unrelated: editor restore + TS intelligence), 9 skipped | /tmp/pi-e2e-full.log |
| 2026-07-16 | (preview-gate, uncommitted) | `npm test` + `npm run check` | 449 unit passed (17 new: preview-service parsing/attribution, pr-draft builder, CSP pin); prettier + boundary(253) + tsc clean; production build clean | vitest |
| 2026-07-16 | (preview-gate, uncommitted) | `CHARTER_SHOTS=1 playwright test ui-shots-preview` | 11-shot visual walk light+dark (gate tabs, honest empty state, live preview + isolation badge, armed marquee, note card, compact room feedback with burned-in selection, checks Unverified, PR draft card, timeline draft entry) reviewed; one usability fix applied from the review: feedback bubble leads with the note, coordinate block folded | /tmp/ui-shots/pg-\*.png |
| 2026-07-15 | (external-cli-rev2, uncommitted) | `PI_IDE_REAL_EXTERNAL_CLI=1 playwright test external-cli-real` (manual, real CLIs) | 3 passed — REAL claude 2.1.210 interactive (nvm/npm shape, comm=claude.exe): detect-in-place bar → user promote → TUI renders ≥80col in 600px panel → typed probe echoed in composer → /exit → panel ended state → return-to-dock (bar ended); REAL claude -p haiku: edit accounted → REVIEW_READY; REAL codex 0.144.1 interactive: detect → promote → update-menu/trust gauntlet driven by keystrokes → probe echoed in composer | /tmp/live-e2e/\*.png |
| 2026-07-15 | (external-cli-rev2, uncommitted) | `npx playwright test` (full e2e) | 76 passed, 0 failed, 9 skipped (3.5m) — includes rewritten external-cli.spec (decorate-in-place → user promote → PTY echo → no auto-return → return click → room/peek) and new terminal-remount.spec (tab A→B→A, Home⇄Editor round-trip) | playwright |
| 2026-07-15 | (external-cli-rev2, uncommitted) | `npm test` + `npm run check` | 346 unit passed; prettier + boundary(212) + tsc clean | vitest |
| 2026-07-15 | (external-cli-rev2, uncommitted) | Finding: prior native-installer e2e fixture (`/bin/zsh` copy) is SIGKILLed by macOS AMFI (platform-binary copy) — that spec could not have run its agent; replaced with a node-binary copy. Also: `external-cli.spec.ts:89` (promoted-scrollback assertion) was red at HEAD b7e787e — 决策 4 UI shipped against a failing gate | reproduced, then fixed | this row |
| 2026-07-15 | (room-preset-e, uncommitted) | `CHARTER_TEST_KEY/BASEURL … playwright test real-room-e` (manual, real gateway) | 1 passed (1.1m) — real write task + follow-up run on anthropic/claude-haiku-4-5; files asserted on disk; preset-E styles asserted computed; focus-outline fix verified | /tmp/ui-shots/e-\*.png |
| 2026-07-15 | (room-preset-e, uncommitted) | `playwright test room-reviewbar room-peek m6-agent-ask` | 8 passed (mock runtime, timeline regrouping regression) | playwright |
| 2026-07-15 | (room-preset-e, uncommitted) | `npm test` + `npm run check` | 345 unit passed; prettier + boundary(211) + tsc clean | vitest |
| 2026-07-15 | (external-cli-detect-fix, uncommitted) | `PI_IDE_REAL_EXTERNAL_CLI=1 playwright test external-cli-real` (manual, real CLIs) | 2 passed — REAL claude 2.1.209 (native installer, kernel comm = version string): detect ✳+EXT+toast → haiku `-p` edit accounted (e2e-touch.txt created) → REVIEW_READY; REAL codex 0.142.4 (user zsh-function wrapper → nvm shim): detect + external task armed | /tmp/live-e2e/\*.png |
| 2026-07-15 | (external-cli-detect-fix, uncommitted) | `playwright test external-cli` | 2 passed — original lifecycle + new native-installer shape (version-named binary `fakeclaude → versions/9.9.9`, kernel comm ≠ CLI name, argv-fallback detection → accounting) | playwright |
| 2026-07-15 | (external-cli-detect-fix, uncommitted) | `npm test` + `npm run check` | 345 unit passed (8 new: findAgentInTable + pollOnce gating); boundary(211) + tsc clean; changed files prettier-clean (an untracked design HTML draft fails repo-wide prettier, pre-existing) | vitest |
| 2026-07-14 | (external-cli, uncommitted) | `npx playwright test` (full e2e) | 69 passed, 1 failed (p2 ⌘K keyboard — documented load-flake class, passes standalone ×1 right after), 5 skipped | playwright |
| 2026-07-14 | (external-cli, uncommitted) | `playwright test external-cli` | 1 passed — full ADR-0017 lifecycle on a real PTY (detect → badge → room → accounting → peek → REVIEW_READY → honest changeSet) | playwright |
| 2026-07-14 | (external-cli, uncommitted) | `npm test` + `npm run check` | 337 unit passed (16 new); prettier + boundary(211) + tsc clean | vitest |
| 2026-07-14 | (fix-init, uncommitted) | `npm test` | 320 passed (init run-once 2 new) | vitest |
| 2026-07-14 | (fix-init, uncommitted) | `playwright test m2-shell m3-editor` + dev relaunch | 5/5 passed; startup log clean of MaxListenersExceededWarning | playwright |
| 2026-07-14 | (room-ending, uncommitted) | `playwright test room-reviewbar pivot-shell shell-v4 m6-agent-ask m9-verification` | 17 passed (2 new) | playwright |
| 2026-07-14 | (room-ending, uncommitted) | `PI_IDE_SOAK=1 playwright test soak` | 1 passed — 50 tasks, rollback via review-bar overflow, one worker, 0 restarts | playwright |
| 2026-07-14 | (room-ending, uncommitted) | `npm test` + boundaries + tsc | 321 passed (mock setSessionModel new); boundary(205)+tsc clean; prettier clean on all changed files | vitest |
| 2026-07-14 | (shell-v5) | `npm run test:e2e` (full) | 65 passed, 1 load-flake (m5-git E2E-008 — passes standalone ×2 and inside suite runs; documented flake class) | playwright |
| 2026-07-14 | (shell-v5) | `playwright test room-peek shell-v3 shell-v4 home-v2 home-dragref p2-visibility` | 2 new + 23 regression passed (worktree peek assertions added to shell-v4) | playwright |
| 2026-07-14 | (shell-v5) | `npm test` + `npm run check` | 303 passed (peek reducer 10 new); prettier+boundary+tsc clean | vitest |
| 2026-07-14 | (shell-v5) | `CHARTER_SHOTS=1 … ui-shots-v5` | visual walk pass (light+dark; peek diff/file/tabs). Known capture artifact: after an emulated mid-session theme flip, full-page CDP captures can show a stale light layer over the peek column — element-level captures and computed styles prove correct dark rendering (probe evidence); not user-visible app state | /tmp/ui-shots/v5-\*.png |
| 2026-07-14 | (ux-dragref, uncommitted) | `playwright test home-dragref` + touched-area regression (home-v2, shell-v3, shell-v4, pivot-shell, p3-light-edit) | 3 new + 18 regression passed | playwright |
| 2026-07-14 | (ux-dragref, uncommitted) | `npm run check` | prettier + boundary(190) + tsc clean | — |
| 2026-07-14 | (ux-dragref, uncommitted) | screenshot walk (temp spec, removed) | sidebar entry, dialog, drag-over ring, file+dir chips, room inline @ reviewed | /tmp/ui-shots/dr-\*.png |
| 2026-07-14 | (p4) | `npm run test:e2e` (full) | 60 passed, 0 failed (soak/real-gateway/ui-shots gated) | playwright |
| 2026-07-14 | (p4) | `npm test` + `npm run check` | 293 passed; prettier+boundary(189)+tsc clean | vitest |
| 2026-07-14 | (p4) | `CHARTER_SHOTS=1 … ui-shots-v4` | visual walk pass — composer 4-mode segment verified compact | /tmp/ui-shots/v4-\*.png |
| --- | --- | --- | --- | --- |
| 2026-07-14 | (ux-polish, uncommitted) | `npm run test:e2e` (full) | 54 E2E passed (soak/real-gateway/ui-shots skipped) | playwright |
| 2026-07-14 | (ux-polish, uncommitted) | `npm test` + `npm run check` | 268 passed; prettier+boundary(181)+tsc clean | vitest |
| 2026-07-13 | (shell-v3 P5) | `playwright test` (full) | 48 E2E passed, 0 failed (soak + credential-gated real-gateway skipped); singles that flaked under load pass standalone ×2 | playwright |
| 2026-07-13 | (shell-v3 P5) | real-gateway.spec (manual, real credentials) | PASS — key+baseUrl stored, 25 models fetched through the gateway, real ask task on anthropic/claude-haiku-4-5 answered with usage/cost from the pi runtime | /tmp/ui-shots/real-\*.png |
| 2026-07-13 | (shell-v3 P5) | `npm test` + `npm run check` | 263 passed; prettier+boundary(177)+tsc clean | vitest |
| 2026-07-13 | (shell-v3 P4) | `playwright test` (full) | 46 E2E passed ×2 consecutively (soak skipped); one earlier run flaked under heavy machine load only | playwright |
| 2026-07-13 | (shell-v3 P4) | `npm test` + `npm run check` | 262 passed; prettier+boundary(173)+tsc clean | vitest |
| 2026-07-13 | (M10) | `playwright test` (full) | 43 E2E passed ×2 (soak opt-in skipped) | playwright |
| 2026-07-13 | (M10) | `test:soak` (50 tasks) | 1 passed — one worker, 0 restarts, clean exit | playwright |
| 2026-07-13 | (M10) | `npm test` + `npm run check` | 253 passed; prettier+boundary(168)+tsc clean | vitest |
| 2026-07-13 | (home-v2 P3) | `playwright test` (full) | 40 E2E passed ×3 consecutively (logs /tmp/e2e-f\*.log) | playwright |
| 2026-07-13 | (home-v2 P3) | `npm test` + `npm run check` | 252 passed; prettier+boundary(167)+tsc clean | vitest |
| 2026-07-13 | (home-v2 P2) | `npm test` | 252 unit/integration passed (34 files) | vitest |
| 2026-07-13 | (home-v2 P2) | `playwright test` (full) | 38 E2E passed ×3 consecutively (one unidentified flake in an earlier run, not reproduced) | playwright |
| 2026-07-13 | (home-v2 P1) | `npm test` | 251 unit/integration passed (34 files) | vitest |
| 2026-07-13 | (home-v2 P1) | `playwright test` (full) | 35 E2E passed, twice consecutively | playwright |
| 2026-07-13 | (pivot) | `npm test` | 238 unit/integration passed (32 files) | vitest |
| 2026-07-13 | (pivot) | `playwright test` (full) | 32 E2E passed, twice consecutively | playwright |
| 2026-07-13 | (M9) | `npm test` | 234 unit/integration passed (31 files) | vitest |
| 2026-07-13 | (M9) | `npm run check` | prettier + boundary(147) + tsc clean | — |
| 2026-07-13 | (M9) | `playwright test` (full) | 29 E2E passed, twice consecutively | playwright |
| 2026-07-13 | (M8) | `npm test` | 226 unit/integration passed (30 files) | vitest |
| 2026-07-13 | (M8) | `playwright test` (full) | 26 E2E passed | playwright |
| 2026-07-13 | (M7) | `npm test` | 201 unit/integration passed (27 files) | vitest |
| 2026-07-13 | (M7) | `playwright test` (full) | 22 E2E passed (clean machine) | playwright |

## Requirement exceptions

None. Any exception requires an ADR and product-owner approval.
